main(num) {
  count = 0;
  n = 1;
  while count < num {
    n = n + 1;
    is_prime = 1;
    d = 2;
    while d < n {
      if (n / d) * d == n {
        is_prime = 0;
      }
      d = d + 1;
    }
    if is_prime == 1 {
      count = count + 1;
    }
  }
  return n;
}